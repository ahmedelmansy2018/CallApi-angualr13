import { PasswordMatch.Validator } from './password-match.validator';

describe('PasswordMatch.Validator', () => {
  it('should create an instance', () => {
    expect(new PasswordMatch.Validator()).toBeTruthy();
  });
});
