import { ExistEmail.Validator } from './exist-email.validator';

describe('ExistEmail.Validator', () => {
  it('should create an instance', () => {
    expect(new ExistEmail.Validator()).toBeTruthy();
  });
});
